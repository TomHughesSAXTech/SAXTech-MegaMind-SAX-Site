# Secret Scanning Configuration
# This file defines patterns that should be allowed in this backup repository

paths-ignore:
  # Ignore all backup directories since they contain legitimate Azure configurations
  - 'azure-backups/**'
  - 'staticwebapps_backup_*.tar.gz'
  - 'functionapps_backup_*.tar.gz'
  - 'n8n_backup_*.tar.gz'

# Note: These paths contain legitimate Azure configuration backups that 
# necessarily include API keys and secrets for disaster recovery purposes